# T0 — Correct stale status and story claims in the docs

[← index](README.md) · source: [full plan](../2026-09-04-nl-task-capture-and-eval-harness.md)

---

**Type:** docs
**Goal:** the architecture docs describe the status and story behaviour the code actually has, so
nobody plans against a data model that changed two features ago.
**Difficulty:** easy
**Depends on:** none
**Acceptance criteria:**
- [ ] `docs/architecture.md` describes per-project custom statuses (`project_statuses` table, slug
      validated by `validate_status_slug`), not a fixed five-value `StatusEnum`
- [ ] The seeded defaults are listed correctly: `to_do`, `in_progress`, `in_review`, `done` — no
      `in_testing`
- [ ] Both `docs/architecture.md` and `CLAUDE.md` state that project-level tasks are assigned to the
      project's default **Backlog** story (`is_default=True`), not `story_id IS NULL`
- [ ] `README.md`'s status arrow line is corrected the same way
- [ ] The `(project_id, story_id, task_id)` and cursor-pagination sections are left alone — they are
      still accurate
**Files in scope:** `docs/architecture.md`, `CLAUDE.md`, `README.md`
**Constraints:** verify every claim against `app/api/services/project_status_service.py` and
`task_service.create_task_for_project` before writing. Do not "fix" the code to match the docs.
**Verification:** `grep -rn "in_testing\|story_id IS NULL" docs/ CLAUDE.md README.md` returns nothing
except deliberate historical notes.
**Out of scope:** any code change; documenting the capture feature (that is T9).

---
